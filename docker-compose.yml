version: "3.8"

services:
  mysql:
    build:
      context: ./
      dockerfile: DB.Dockerfile
    ports:
      - "3306:3306"
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
